# {{project_name}} — Convenciones

*(Este documento define convenciones de código, nombrado y estructura. Lo mantienen los agentes `architect` y `developer`.)*

## Convenciones de código

*(Completar: estilo de código, formatter, linter y guías del lenguaje.)*

## Nombrado

*(Completar: convenciones de nombres para archivos, variables, funciones, ramas y commits.)*

## Estructura de carpetas

*(Completar: estructura esperada y qué vive en cada carpeta.)*

## Proceso de trabajo

Este proyecto opera con el framework multiagente documentado en la referencia `framework` (véase `AGENTS.md`). Las reglas de proceso, delegación, gates y contratos viven en el framework; este archivo solo registra las convenciones específicas de {{project_name}}.
